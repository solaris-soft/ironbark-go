version: "2"
sql:
  - engine: "postgresql"
    queries: "./schema/query.sql"
    schema: "./schema/schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v5"
